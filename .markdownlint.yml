default: true
MD013: false  # line length
MD024: false  # duplicate headings (CHANGELOG files generated by release-please repeat section names across versions)
MD033: false  # inline HTML (used in ADRs)
MD036: false  # emphasis used as heading (used in PRD/architecture docs for intentional styling)
MD041: false  # first line should be a top-level heading (some files start with frontmatter)
MD060: false  # table column style (pre-existing tables use mixed styles; purely cosmetic)
MD012: false  # release-please generates CHANGELOG.md files with double blank lines between version sections
