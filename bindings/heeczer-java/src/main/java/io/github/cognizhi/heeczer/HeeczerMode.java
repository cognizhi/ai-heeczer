package io.github.cognizhi.heeczer;

/** SDK execution mode. */
public enum HeeczerMode {
    /** HTTP/image mode: call the ingestion service. */
    image,
    /** Native mode: reserved for the future JDK FFM binding. */
    native_mode
}
